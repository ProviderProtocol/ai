# v0.0.33 Changelog

## Fixes

- [x] Fixed: Missing `./responses` subpath export in package.json

### Usage

The responses module can now be imported directly:

```ts
import { ... } from '@providerprotocol/ai/responses';
```
